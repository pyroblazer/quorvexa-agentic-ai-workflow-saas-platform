{
  "buildCommand": "pnpm --filter @quorvexa/observability run build && pnpm --filter workflow-service run build",
  "installCommand": "npm i -g pnpm@9 && pnpm install",
  "framework": null,
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
