{
  "buildCommand": "corepack enable && pnpm install && pnpm --filter workflow-service run build",
  "installCommand": "corepack enable && pnpm install",
  "framework": null,
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
