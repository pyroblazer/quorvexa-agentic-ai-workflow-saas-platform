{
  "buildCommand": "corepack enable && pnpm install && pnpm --filter notification-service run build",
  "installCommand": "corepack enable && pnpm install",
  "framework": null,
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
